[package]
name = "hypergrab"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.29"
calamine = "0.24" # Excel reader
xcap = "0.0.14"   # Cross-platform screen capture
image = "0.25"    # Image processing
rfd = "0.14"      # File dialogs
